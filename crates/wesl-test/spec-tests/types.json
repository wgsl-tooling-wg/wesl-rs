[
    {
        "name": "mat-i32",
        "desc": "matrix type must be float",
        "kind": "context",
        "code": "const m = mat2x2(1i, 0, 0, 0);",
        "lower": true,
        "expect": "fail"
    },
    {
        "name": "mat-i32-implicit",
        "desc": "matrix type must be float",
        "kind": "context",
        "code": "const m: mat2x2<i32> = mat2x2(1, 0, 0, 0);",
        "lower": true,
        "expect": "fail"
    },
    {
        "name": "mat-i32-assign",
        "desc": "matrix type must be float",
        "kind": "context",
        "code": "@fragment fn f() { var m = mat2x2(1, 0, 0, 0); m[0][0] = 1i; }",
        "lower": true,
        "expect": "fail"
    },
    {
        "name": "mat-assign-vec-abstractint",
        "desc": "matrix type must be float",
        "kind": "context",
        "code": "@fragment fn f() { var m = mat2x2<f32>(); m[0] = vec2(1, 0); }",
        "lower": true,
        "expect": "pass"
    }
]
